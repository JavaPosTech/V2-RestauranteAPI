services:
  postgres:
    image: postgres:18
    container_name: postgres
    environment:
      POSTGRES_PASSWORD: fiap@2026
    ports:
      - "8745:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data: